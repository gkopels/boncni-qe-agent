# OpenPE EVPN tenant customer-b (L3 VRF 1002 + L2 LAN 1102)
apiVersion: network.openperouter.io/v1alpha1
kind: L3VNI
metadata:
  name: customer-b
  namespace: openshift-openperouter
spec:
  vni: 1002
  vrf: customer-b
  underlayAddressFamily: IPv4
  importRTs:
    - "64514:1002"
  exportRTs:
    - "64514:1002"
  nodeSelector:
    matchLabels:
      node-role.kubernetes.io/worker: ""
  hostSession:
    asn: 64514
    hostASN: 64515
    localCIDR:
      ipv4: 172.16.21.0/24
---
apiVersion: network.openperouter.io/v1alpha1
kind: L2VNI
metadata:
  name: customer-b-l2
  namespace: openshift-openperouter
spec:
  vni: 1102
  underlayAddressFamily: IPv4
  gatewayIPs:
    - 172.16.2.1/24
  routingDomain:
    type: L3VNI
    l3vni:
      name: customer-b
  hostMaster:
    type: LinuxBridge
    linuxBridge:
      lifecycle: Managed
  nodeSelector:
    matchLabels:
      node-role.kubernetes.io/worker: ""
---
apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: customer-b-macvlan
  namespace: customer-b
spec:
  config: |-
    {
      "cniVersion": "0.3.1",
      "type": "macvlan",
      "master": "br-hs-1102",
      "mode": "bridge",
      "ipam": {
        "type": "static",
        "routes": [
          { "dst": "0.0.0.0/0", "gw": "172.16.2.1" }
        ]
      }
    }
---
apiVersion: v1
kind: Pod
metadata:
  name: customer-b-l2-w0
  namespace: customer-b
  annotations:
    k8s.v1.cni.cncf.io/networks: |-
      [{
        "name": "customer-b-macvlan",
        "namespace": "customer-b",
        "ips": ["172.16.2.10/24"]
      }]
spec:
  nodeSelector:
    kubernetes.io/hostname: worker0
  containers:
    - name: net
      image: quay.io/ocp-edge-qe/eco-gotests-network-client:v4.20
      command:
        - /bin/sh
        - -c
        - ip r del default dev eth0 || true; sleep infinity
      securityContext:
        capabilities:
          add: ["NET_ADMIN", "NET_RAW"]
---
apiVersion: v1
kind: Pod
metadata:
  name: customer-b-l2-w1
  namespace: customer-b
  annotations:
    k8s.v1.cni.cncf.io/networks: |-
      [{
        "name": "customer-b-macvlan",
        "namespace": "customer-b",
        "ips": ["172.16.2.11/24"]
      }]
spec:
  nodeSelector:
    kubernetes.io/hostname: worker1
  containers:
    - name: net
      image: quay.io/ocp-edge-qe/eco-gotests-network-client:v4.20
      command:
        - /bin/sh
        - -c
        - ip r del default dev eth0 || true; sleep infinity
      securityContext:
        capabilities:
          add: ["NET_ADMIN", "NET_RAW"]
