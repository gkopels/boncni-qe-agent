# Archived manifests

These were intermediate lab files superseded by the canonical set in the parent directory:

- ToR: use `../30-tor-evpn-vxlan.yaml` (not `10-tor-frr.yaml`)
- customer-a EVPN: use `../31-evpn-customer-a.yaml` + `../04-workload-customer-a.yaml`
- customer-b EVPN: use `../41-evpn-customer-b.yaml` + `../16-svc-customer-b.yaml`

Kept for reference only. Do not apply unless comparing history.
