apiVersion: v1
kind: Namespace
metadata:
  name: customer-a
  labels:
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/audit: privileged
    pod-security.kubernetes.io/warn: privileged
---
apiVersion: v1
kind: Service
metadata:
  name: customer-a-web
  namespace: customer-a
  labels:
    app: customer-a-web
  annotations:
    metallb.universe.tf/address-pool: pool-customer-a
spec:
  type: LoadBalancer
  selector:
    app: customer-a-web
  ports:
    - name: http
      port: 80
      targetPort: 8080
---
apiVersion: v1
kind: Pod
metadata:
  name: customer-a-l3-w0
  namespace: customer-a
  labels:
    app: customer-a-web
spec:
  restartPolicy: Always
  nodeSelector:
    kubernetes.io/hostname: worker0
  containers:
    - name: net
      image: quay.io/ocp-edge-qe/eco-gotests-network-client:v4.20
      ports:
        - containerPort: 8080
          name: http
      command:
        - /bin/bash
        - -lc
        - |
          testcmd -listen -protocol tcp -port 8080 -interface eth0 &
          sleep infinity
      securityContext:
        capabilities:
          add: ["NET_ADMIN", "NET_RAW"]
---
apiVersion: v1
kind: Pod
metadata:
  name: customer-a-l3-w1
  namespace: customer-a
  labels:
    app: customer-a-web
spec:
  restartPolicy: Always
  nodeSelector:
    kubernetes.io/hostname: worker1
  containers:
    - name: net
      image: quay.io/ocp-edge-qe/eco-gotests-network-client:v4.20
      ports:
        - containerPort: 8080
          name: http
      command:
        - /bin/bash
        - -lc
        - |
          testcmd -listen -protocol tcp -port 8080 -interface eth0 &
          sleep infinity
      securityContext:
        capabilities:
          add: ["NET_ADMIN", "NET_RAW"]
